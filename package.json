{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.4.3"
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "npx lerna run build"
  }
}
